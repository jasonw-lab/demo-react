networks:
  youlai-boot:
    external: 
      name: docker_youlai-boot

services:

#  # Frontend service (nginx)
#  photo-frontend:
#    image: nginx:alpine
#    ports:
#      - "80:80"
#    volumes:
#      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
#      - ${DATA_PATH}/nginx/html:/usr/share/nginx/html
#    depends_on:
#      - photo-api

  # Backend service (API)
  photo-gallary:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: demo-photo-gallary
    ports:
      - "3001:3001"
    env_file:
      - ./photo-gallary/.env.production
    environment:
      - NODE_ENV=production
      - NEXT_BASEPATH=/photo-gallary
#    extra_hosts:
#      - "host.docker.internal:host-gateway"
    networks:
      - youlai-boot
    external_links:
      - youlai-mysql:mysql
      - youlai-minio:minio

        #networks:
      #  youlai-boot:
      #    driver: bridge


